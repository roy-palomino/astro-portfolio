---

interface Props {
  bgColorClass?: string;
}

const { bgColorClass = "bg-white" } = Astro.props;
---

<div class={bgColorClass}>
  <div class="flex flex-col px-4 py-16 space-y-12">
    <slot />
  </div>
</div>
